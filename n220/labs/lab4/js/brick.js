let bricks = [];

function setup(){
    createCanvas(400,400);
    for (i = 0; i < 50; i++) {
      }
  }
  
  function draw(){
    background(0)
    for (i = 0; i < bricks.length; i++) {
        if(mouseIsPressed{
            let brick = rect(mouseX, mouseY, 20, 50)
            brick.push[i]
        })

        }
    }

